interface:
  display_name: "Subagent Task Queuer"
  short_description: "Queue subagent work and take over unfinished tasks"
  default_prompt: "Use $subagent-task-queuer to delegate bounded work, keep the main path moving, and take over unfinished work safely."
