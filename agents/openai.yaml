interface:
  display_name: "Subagent Task Queuer (Router)"
  short_description: "Route subagent start and takeover phases"
  default_prompt: "Use $subagent-start with a task to let the main agent work when idle, or spawn a subagent under compound-engineering:lfg when the main path is active."
